# Express + Flask Docker Project

This project contains:
- Express frontend
- Flask backend
- Docker containers
- Docker Compose networking

Run using:

```bash
docker-compose up --build
Frontend: http://localhost:3000
Backend: http://localhost:5000
